package transition.data;

/**
    Functionally the same as `BaseTransition` but it is not the base.

    That way there is a specific instant transition instead of needing to use the base. 
**/
class InstantTransition extends BaseTransition{}